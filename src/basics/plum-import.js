export const plum_import = {
  '[import]: Import basics plum module': {
    prefix: 'plum-import-basics',
    body: ["@import 'plum-basics';"],
    description: '[import]: Import basics plum module',
  },

  '[import]: Import milligrid (flexbox) module from plum': {
    prefix: 'plum-import-milligrid',
    body: ["@import 'plum-milligrid';"],
    description: '[import]: Import milligrid (flexbox) module from plum',
  },

  '[import]: Import semantic-ui (min) module from plum': {
    prefix: 'plum-import-semantic',
    body: ["@import 'plum-semantic';"],
    description: '[import]: Import semantic-ui (min) module from plum',
  },

  '[import]: Import bootstrap module (min) from plum': {
    prefix: 'plum-import-bt',
    body: ["@import 'plum-bootstrap';"],
    description: '[import]: Import bootstrap module (min) from plum',
  },

  '[import]: Import bootstrap (grid) module from plum': {
    prefix: 'plum-import-bt-grid',
    body: ["@import 'plum-bootstrap-grid';"],
    description: '[import]: Import bootstrap (grid) module from plum',
  },

  '[import]: Import bootstrap module (utils) from plum': {
    prefix: 'plum-import-bt-utils',
    body: ["@import 'plum-bootstrap-utils';"],
    description: '[import]: Import bootstrap module (utils) from plum',
  },
}
